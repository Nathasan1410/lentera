<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".homepage.CreateFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="8dp"
        android:layout_marginTop="56dp"
        android:layout_marginBottom="56dp"> <!-- Adjust margin to match toolbar height -->

        <!-- Container for color input and apply button -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_gravity="top"
            android:padding="8dp">

            <EditText
                android:id="@+id/colorInput"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:hint="Enter Hex Color Code (e.g., #FF0000)"
                android:inputType="text" />

            <Button
                android:id="@+id/btnApplyColor"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Apply Color"
                android:layout_marginStart="8dp" />
        </LinearLayout>

        <!-- Scrollable grid layout -->
        <HorizontalScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <androidx.gridlayout.widget.GridLayout
                android:id="@+id/ledGrid"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:columnCount="8"
                app:orientation="vertical"
                app:rowCount="8" />
        </HorizontalScrollView>

        <!-- Button to change input mode -->
        <Button
            android:id="@+id/btnChangeInputMode"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Change Input Mode"
            android:padding="10dp"
            android:layout_marginTop="16dp"
            android:layout_gravity="center_horizontal" />
    </LinearLayout>
</ScrollView>
